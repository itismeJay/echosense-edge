pyaudio
numpy
ai-edge-litert
faster-whisper
# RealtimeSTT handles mic capture + WebRTC/Silero VAD + faster-whisper streaming
# (replaces audio/capture.py + audio/vad.py for the live path). silero-vad ships
# the VAD model locally so RealtimeSTT never falls back to interactive torch.hub.
RealtimeSTT
silero-vad
noisereduce
rapidfuzz
requests
# vosk — legacy ASR path (model/vosk_detect.py), no longer used at runtime
vosk
